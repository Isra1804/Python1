#Create a function called "calculate" 
#Give it three arguments (three numbers)
def calcul(number1, number2, number3): #Add the first two numbers and multiply them by the third number and Make sure the addition happens first.

    return (number1 + number2) * number3 #Return the value of the function "calculate".

#Print the value of the "calculate" function + Call the function three times.

print (calcul(1, 2, 3))
print (calcul(10, 20, 30))
print (calcul( 100, 200, 300))
